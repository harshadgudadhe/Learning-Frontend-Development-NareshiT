------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 1 (Introduction)
Date: 09/09/25
------------------------------------------------------------------------------------------------------------------------------------

                                                JavaScript
                                            ------------------
JavaScript Use to make website functional/Interactive.
Python : AI
JavaScript : Web Devlopement  
            --> React.js, TypeScript, Angular, Node.js, Next.js.
Java : Enterprise Application 
website:

Syllabus:
---------
        1. Basic Javascript
                |- Introduction
                |- Data-types
                |- operator
                |- control-statements
                |- loops
                |- function
                |- Arrays
                |- Object

        2. DOM (Data Object Module)
                |- DOM Introduction, Selection
                |- DOM Manipulation
                |- DOM Creation, Deletion
                |- Events
                |- Form Validations
                |- Rest bs spread, this Keywords
                |- ES6 features
                |- OOP's
        
        3. Advace JavaScript
                |- call back hell, promises, fetch, API, Asynch await
                |- libraries(jQuery,axios)
                |- 


1. Introduction:
----------------
                >JavaScript is a client-side programming language, 
                >Used to build Interactive website using HTML, CSS.
                >JavaScript controls the behaviour of web-pages. 
                >Provide functionality to the web-pages.

    Initially JS was created in 1995(4th dec 95) by Breden Eich while working at "Netscape(browser)" just in 10 days.
    Initially it's named as "Mocha", later changed as "LiveScript".
    but java was got famous becuase Java have feature "WORA" or "Plat-form Independece".
    To use the popularity of Java, Livescript renamed as JavaScript.

    Java = c  +  c++.
    JavaScript = Python + Java.

    JavaScript runs in the browsers because every browser contains engine to run JavaScript called as "JS Engine."
    Note: Different browsers have different Js Engine like: Google Chrome, Opera have "V8 JS Engine"
          v8(Chrome). 

    JavaScript Also runs on server-side which is "Node.js", 
    so there will be question that 
    => can we run javascript on server-side.
    &
    => can we run javascript without Browse.

JavaScript is high-level, dynamic programming language that makes website alive.
Think building website is like building house
where:
        HTML --> Used for structure of website or visual presentation like walls, windows, doors etc
        CSS  --> used to paint or design the website or how it should looks like: painting,style, decoration etc.
        JS   --> used to make website interactive or how should it works. like: Electricity etc.    


=> How to include JS in HTML?
we can include JS In html in two ways.
They are :
       => Internal Js
       => External Js

       => Internal Js: we can include JavaScript using <script> tag or element.
          Example: 
                   <script>
                      // JS Code or logic
                   </script>
       => External Js: We can include external JavaScript file(.js file) to Use HTML using <script> tag with src="path" attribute.
          Example: 
                   <script src="filepath"> </script>

=> Comments in Js: 
            There are two types of comments in JS are 
            1. Single-line :  //  -----

            2. Multi-line :  /* ----- */
    Notes: comments are used to make code readable for user/developer.


=> Variables in Js:
------------------- 
        In js we can declare variable using keywords such as "var" , "let" and "const"
        Variables are used to store the values.
        
        Difference between Var, let and const:
        1. Var : can be re-declare & re-initialise. var variables are function scoped. var is out-dated now it's not used in current scenarion.  
                 example: var a=5; var a=10;
        2. Let : can be re-initialise but can't re-declared. let variables are block scoped. 
                 example: let a=5; a=10;
        3. Const : Cant be re-declared or re-initialise.
                 example: const a=5;    but can't re-initialise. const variables are block scoped. 

Note: JavaScript is case-sensitive language.

To accept input, we use one method called "prompt". but prompt accepts only strings. and this method provided by browser.
example:
--------
        let a= +prompt("enter a value");
        
------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 2 (Data-types)
Date: 10/09/25
------------------------------------------------------------------------------------------------------------------------------------

Data-Types:
-----------
        JavaScript is Dynamic programming language.
        It means in JS, There is no-fixed  data-type to a variable. 
        The data-type of a variable is decided by value it is holding.
        
        There are two categories of data-types:
        1. Primitive data-type
        2. Non-Primitive data-type

        1. Primitive data-type: 
                These datatypes holds the value and compares only value. These datatypes are immutable.
                There are 5 basic Primitive datatypes such as:
                -> Number
                -> String
                -> Boolean
                -> Undefined
                -> Null

        2. Non-Primitive data-type: 
                These datatypes holds the value and compares the address/reference. These datatypes are Mutable.
                There are basic Primitive datatypes such as:
                -> Functions
                -> Arrays
                -> Objects 

Can we change one datatype into another datatype?
yes, The process of changing one datatype to another datatype is called "Type-casting",
There are two types of type-casting: 

1. Implicit type-casting
2. Explicit type-casting

1. Implicit type-casting: 
            It's the default or automatic conversion of datatype based on operation.
            Example: 
                     5 + "4" ==> "105"
                     2 + true ==> 3
                     5 - "4"  ==> 1
                     5 - true ==> 4
                     5 * "4"  ==> 20


1. Explicit type-casting: 
            It's the forced way of converting one data-type to another.
            Example:
                    5 + +"4" ==> 9
                    5 + Number('4') ==> 9


                                                number
                                        -----------------------
1. number: Any numerical value is a number.
        Every number can have some default methods such as 
        toString, toPrecision etc..,
        numbers also have math methods such as 
        sqrt, cbrt, pow, ceil, floor, toFixed  etc..,


------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 3
Date: 11/09/25
------------------------------------------------------------------------------------------------------------------------------------

toFixed(): used to return, round-up value of N-decimal digits.
toPrecision(): used to return round-up value of N-total digits
toString(): used to convert number into a string and different base-system.
example:
        let x=25.4164
        x.toFixed(2)     ==> 25.41
        x.toPrecision(3) ==> 25.4
        x=25;
        x.toString(2)    ==> 11001           //binary


=> Most used math-methods are: 
Math.sqrt(16): returns square-root of 16 ==> 4
Math.cbrt(27): returns cube-root of 27 ==> 3
Math.pow(2,3): returns power of 3 ==> 8                   //note: we can write same thing like 2**3 for power in JS only.
Math.abs(-5): returns absolute value, always positive ==> 5
Math.max(1,2,3,4,5): returns maximum value ==> 5
Math.min(1,2,3,4,5): returns minimum value ==> 1
Math.ceil(2.1): returns nearest greater value ==> 3
Math.floor(2.1): returns nearest smaller value ==> 2
Math.round(1.4): returns nearest integer ==> 1
Math.random(): returns random value in between 0.1 & 0.9  ==>  (un-predictable) 


                                                        String
                                                ---------------------
2. String:  String is collection of characters.
----------  eg:  "abc124", "abc 124"

In javascript string can base

String can also dynamic, called "template literals". It uses backticks for string and ${} for dynamic value. 
let x="john"
console.log(`x= ${x}`);

String methods:
        1. toUpperCase(): used to convert string into upper-case.
        2. toLowerCase(): used to convert string into lower-case.
        Note: Original String will never change since strings are immutable.
        3. length       : used to return the number of characters in string.
        4. charAt(index): used to return character that present on number of index which is provided in method parameter.
        5. indexOf('ch'): used to return.
        6. lastIndexOf('c'):
        7. startsWith('string'):
        8. endsWith('string'):
        9. slice(start,end): used to return substring from index s to index e-1.
        10.slice(start): used to return substring from index s to end.
        11.split(value): used to split the string based on value v and return an array of substring.
        eg: "prasad".split('a') ==> ['pr', 's', 'd']
        12.replace(old,new): used to replace first occurance of old value to new value. 
        13.replaceAll(): we can use replaceAll. for all occurances. 
        14.trim(): used to return string after removing the spaces from starting and ending of string.


3. Boolean: Used to store true/false. If converted to number, true is converted as 1 and false is converted as.

4. undefined: Primitive datatype/value taht represents un-initialised or non-existent element.
eg: 
    let y;
    console.log(z)             // undefined

    undefined expects future occupance of value.

5. null: also the primitive value that indicates a variable has no value or no object.


------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 4 (Operators)
Date: 12/09/25
------------------------------------------------------------------------------------------------------------------------------------

Operators: Used to perform the operations. 
        There are different types of operators such as: 

1. Arithmetic operator: 
        +,  -  ,  *  ,  / (direct division) ,   % (remainder)  , **(Exponentiation or power)

2. Relational Operators: used for comparison.
   >  :
   
   <  :
   
   >= :
   
   <= :

   == : will compare only values not data-types.

   === : will compare both values & data-types.

   != :  returns negation of "=="

   !== : returns negation of "==="

3. Logical Operators: These operators are used to combining thwe statements logically like:
   
   &&  (Logical AND) : return "true" only if both are true. 
   ||  (Logical OR)  : return "true" if any-one is true.
   !   (Logical NOT) : return "true" if its actually false.(opposite output)

4. increment and decrement: Unary operator which is used to increase and decrease value by 1.

   pre-increment/decrement: first update the value then returns it.
   post-increment/decrement: first returns the value then updates it.  
   b=10; ++b; ==> b=11;
   b=10; b++; ==> b=10;

5. Ternary Operator:  its conditonal operator, which is short-hand operator used to check the condition "true" or "false" and make a decison based on that value.
   let a=10;
   let c=(a>5) ? 20 : 30;


6. nullish coalescing Operator: checks 2 values and         
        returns 1st value if its not undefined or null, 
        else returns 2nd value.
        eg:
            let a=10,b=20;
            let c = a ?? b ;            //10
        eg: 
            let a1=undefined , b1=20;
            let c1 = a1 ?? b1 ;         //20


------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 5 (Control statements)
Date: 15/09/25
------------------------------------------------------------------------------------------------------------------------------------

*Control statements: 
--------------------
1. Decision-Making statements:  
2. Loops

1. Decision Making statements:
-------------------------------      
        Are also called control Statements which are used to control the flow of code based on a condition/decision.
        Most used  decision Making Statements are: 

        *if:
        ---- 
                    i.  simple-if:
                                    if(cond)
                                    {
                                        //Statements
                                    }
                                

                   ii.  if-else: 
                                   if(cond){
                                         //Statements
                                   }
                                   else
                                   {
                                       //Statements 
                                   }
                                

                  iii.  if --> else-if --> else   or else-if  or Ladder-if:
                                   if(cond)
                                   {
                                        //Statements
                                   }else if(cond)
                                   {
                                        //Statements
                                   }
                                   else
                                   {
                                        //Statements
                                   }

                   iv.  Nested if:
                                 if(cond)
                                 {
                                        //Statements
                                 }else{
                                        if(cond)
                                        {

                                        }else{

                                        }
                                 }
                   
------------------------

        *switch:     
        --------
                switch works with multiple conditions using cases. It's an alternative approach on if-else-if or Ladder-if. 
                But Each case should be terminated by using keyword "break" else it falls to the next case and always needs a case for "default" entry

                Syntax:
                         switch(cond)
                         {
                                case value1:   //Statements;  Break;
                                case value2:   //Statements;  Break;
                                case value3:   //Statements;  Break;
                                case value4:   //Statements;  Break;
                         }
           



--------------------------------------------
Nano Banana (hugging face)
How the recommandation system works ?
Payment Gateways
Aauth-gaurd
portfolioLink: <!-- sonuansari1998.github.io/my_portfolio.netlify -->

------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 6 (Loops)
Date: 16/09/25
------------------------------------------------------------------------------------------------------------------------------------

Loops:
------
      Used to iterate certain statements again and again untill the condition fails.

Types of loop:
--------------
two types of loops:
1. Entry control
2. Exit control  

1. Entry control: use to check the condition at the starting of the loop and run statements if condition is true, once the statements are over, it will again check the condition and repeat the process untill the condition false.

                1. while : 
                        Is an entry-control loop which used condition, updation and initialization in 3 different statements: 
                        syntax: initialization;
                                while(condition)
                                {
                                        statements;
                                        updation;
                                }

                        Example: print number 1 - 10 using while.
                                let i=1;
                                while(i<=10)              //condn
                                {
                                        console.log(i);    //statement
                                        i++;               //updation
                                }
               
               3. for : 
                        Is an entry-control loop used to iterate certain statements by using initialization, condition and updation in the same line .
                        syntax: 
                               for(initialization;condition;updation)
                               {
                                //statements
                               }
                        
                        Example1: print alternate letters of given string(input: PRASAD , output:)
                               
                               let x="PRASAD"
                               for(i=0; i<=x.length-1; i+=2)
                               {
                                console.log(charAt(i));
                               }

                        Example2:
                               let arr = [5,4,3,2,1];
                               for(i=0; i<=x.length-1; i++)
                               {
                                console.log(arr[i]);
                               }                               
                Note: This is called standard-for
                      but in javascript there are other variations for such as 


        i. for in: used to iterate array indexes/address.
               syntax:  
                      for(let i in arrayName)
                      {
                        //Statements
                      }
                here, i will be automatically array indexes.
                Example:
                       let arr=[5,4,3,2,1]
                       for(let i in arr)
                       {
                        console.log(i);
                       }                     


        ii for of:  used to iterate array elements
               syntax:  
                      for(let i of arrayName)
                      {
                        //Statements
                      }
                here, i will be automatically array elements.
                Example:
                       let arr=[5,4,3,2,1]
                       for(let i of arr)
                       {
                        console.log(i);
                       } 


Tasks:
     Level 0:
        1. print even numbers from 1-20
        2. print 3 table in console.
     Level 1:
        1. check a number is palindrome or not ?
          
        2. check a string is palindrome
           const s = prompt("eneter a string");
let str = s.toLowerCase();
let revstr='';

for(let i=str.length-1; i>=0; i--)
{
    revstr=revstr+str.charAt(i);
}

if(str==revstr)
{
    console.log(`${str} is palindrome`);
}else{
    {
    console.log(`${str} isn't palindrome`);
}
}


     Level 3: 
        1. P
           P R 
           P R A
           P R A S
           P R A S A
           P R A S A D
----------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 7 (Loops)
Date: 17/09/25
------------------------------------------------------------------------------------------------------------------------------------

Brain-stroming Question:
------------------------

//swapping without third-var
let a=10, b=20;
console.log("a= "+a ,"b= "+b);
a=a+b;
b=a-b;
a=a-b;
console.log("a= "+a ,"b= "+b);

//swapping without third-var & (arithmetic operator) 
let x=10, y=20;
console.log("x= "+x ,"y= "+y);
x=x*y;
y=x/y;
x=x/y;
console.log("x= "+x ,"y= "+y);


2. Exit control:                
    

                2. do-while: do-while is exit control loop which checks the condition at the exit of body. If the condition is true, 
                   it repeat the loop else it terminates. 
                   syntax:
                          //initialization 
                          do{
                                //statements
                                //updation
                          }while(condn)

                   example: print numbers in between 1 to 20 using do while

                   let i=1;
                   do{
                        console.log(i);
                        i++;
                   }while(i<=20);
------------------------------------------------
// To practice javascript:  visit "freecodecamp" website 

------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 8 (Functions)
Date: 18/09/25
------------------------------------------------------------------------------------------------------------------------------------

functions:
----------
           function is a block of code which can be reausable. Instead of implementing  whole code, we can divide the code into logical parts called functions.
           Advantages: 
                1. Reausability
                2. Modularity
                3. 
                4.
          Every function will have  
                function declaration, function definition and function cell.
          Syntax:
                function functionName(parameter)
                {
                        //definition;
                }
                //Function call
                functionName(argument);


          1. Simple function: 
                eg. Write a function that prints "Hello world" and call it ?

                => functions can also accepts parameter which are passed while calling the function.

                =>function can even return the values.
                eg. write a function that accepts a string and returns no.of characters.

          2. Expression Function:
          functions are also treated as a variable in javascript so that whole function definition is stored as a variable. we can even create function expressions.
          eg.


          3. Arrow Function:
          function expressions can be simplified as arrow function, arrow function don't use keyword "function" and "return"
          (if only one statement is there)
          eg.


------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 9 (Functions)
Date: 19/09/25
------------------------------------------------------------------------------------------------------------------------------------

//Brain-Stroming
// 4 3 2 1
// 3 2 1
// 2 1 
// 1



//--------------------------------------------------------------------------------------------

4. callback functions:  functions which are passed as a parameter to another function.
       eg.  
           function mad()                    //
           {
              console.log("I'm mad");
           }
//doubt1
           function mainFun(x)               //eg. likewise Server
           {
                x();
           }
           mainFun(mad);                     //callback function
               |
               |
        higher order funtion 
//-------------
        eg2.

        function raju()
        {

        }

        function message()
        {
                console.log("It's a message");
        }

        function rani(p)
        {
                p();
        } 

        rani(message);                       //                                 
//-------------
        eg3.
        
        function convert(s)
        {
                return s.toUpperCase();
        }
        
        function firstLetter(fun)
        {
                let x = fun("prasad");
                return x[0];
        }
        firstLetter(convert);


//firstLetter( function(x) {return x.toUpperCase()} )

//---------------------------------

       eg4. 

       function greet(name,fun)               //2
       {
        fun(name);                            //3       ==> indirectly  sayName("Prasad");
       }
       function sayName(n)                    //4
       {
        console.log("My name is ${n}");       //5
       }
       greet("prasad", sayName);              //1

--> sayName is function which is passed in the parameter of greet, so sayName is a callback function and greet(function which receives callback) is higher order function.

//==================================================================================================================

**Closures(memorization): Is a memory technique in which inner loops remember the outer loop values even after outer execution ended.
eg1.
    function outer()                          //2
    {
        let a=5;
        function inner()                      //3
        {
            ++a;
            console.log(a);                   //4
        }
//doubt2
        return inner;
    }
    let x = outer();                          //1      //here, it prints inner function definition
    x();              output: 6               //5
    x();              output: 7               //6
    x();              output: 8               //7
//-----------------------------------    
** IIFE(Immediate Invoke Function Expressions): An IIFE is a function that is defined and invoked/called Immediately. It is used to hide the data from global scope.
   eg: 
       (function()
       {
          let password="12345678";
          console.log("Hii!");
       }) ();                              //We can't use it later again, becoz here we don't have name of this function to called.
      
      
      //Trying to recall again 
       console.log(password);              // but it's not possible so, it will generate error;

//--------------------------------------

------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 10(Revision Till the day)
Date: 20/09/25
------------------------------------------------------------------------------------------------------------------------------------

Practice JS on Website: "WayGround.com"

//Task: Convert below simple function into Expression and Arrow function?
 

------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 11(Arrays)
Date: 21/09/25
------------------------------------------------------------------------------------------------------------------------------------

*Arrays:  Array is a collection of elements.
-> Arrays are represent by square brackets and maintain address of every element as 'index'. indexes starts from 0.
-> Arrays are like a hotel with multiple rooms contains each element in a room and each room have a number. Room number starts from 0.
   Eg. let arr= [5,9,6,7,3];
       console.log(arr[0]);           // 5 
       console.log(arr[0] arr[1]);    // 59 
       console.log(arr[20]);          // undefined 
       arr[20]  = 49;                 // length=21

-> Arrays are mutable in js.
   Eg. 

-> Array methods: 
         1. length: length returns the total number of elements (last value index+1)
         eg. console.log(arr.length)
         2. push(values): Used to add elements to the end of the array.
         eg. arr=[21,22,23,24];
             arr.push("raj");
             console.log("arr");               //output: [21,22,23,24,'raj']
         3. pop(values): Used to remove last element of the array.
         eg. arr=[21,22,23,24."raj"];
             arr.pop();
             console.log("arr");               //output: [21,22,23,24]
         4.  unshift(values): used to add elements to the starting of the array.
         eg. arr.unshift(20)
             console.log("arr");               //output: [20,21,22,23,24]
         5.  shift(values): used to remove first element of the array.
         eg. arr.unshift()
             console.log("arr");               //output: [21,22,23,24]
         6.  arr.splice: used to add elements anywhere(any index) in array.
             Syntax: arr.splice( starting index, deleteCount,InsertingValues);
             eg. arr.splice( 1,2);   
             console.log("arr");               //output: [23,24]  
             here it will delete start removing first 2 elements from index 1.
         7.  arr.splice: used to add elements anywhere(any index) in array.
             Syntax: arr.splice( starting index, deleteCount,InsertingValues);
             eg. arr.splice( 1,0,11,12,13);  
             console.log("arr");               //output: [23,11,12,13,24]  
         
         Tasks: 
         let name=['Harsh','Azhar','Mohit','Ayush','Akash','Adarsh'];
         1. delete "Adarsh' and add "Monali".
         eg.
         2. delete "Azhar" and add "Mohitf".
         eg.
         3. add name "Azhar" between  harsh and mohit.
         eg.
         

        8. forEach( callBackFunction(elmnt,index) ): used to pass a callBackFunction to each and every element of array and for every time, it will accept element and index.
           Syntax: 
               arrayName.forEach();
           Eg.
           let arr=[5,4,9,8];
           arr.forEach(function(e,i) 
           {
                 console(e);
           }) 

           Task1:
           //using for each find sum of elements in the array?
           let arr[5,4,3,2,1]
           let sum=0;
           arr.forEach( function(e,i) 
           {
               sum += e;
               console.log(sum);
           })

           Task2:
           let str=" World Health Organization ";
           //Using forEach print "WHO" for given string.
           let words= str.split(' ');
           let abbr='';
           words.forEach(function(e,i)
           {
                abbr = abbr +e.charAt(0)
           })
           console.log(abbr);
//---------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 12(Arrays)
Date: 23/09/25
------------------------------------------------------------------------------------------------------------------------------------

//Use forEach and print each elements cube in given array.

let arr= [5,4,9,8];
arr.forEach(function(ele,indx)
{
    console.log(ele*ele*ele);
            //or
    console.log(ele**3);
})


9. Map(callBackFunction(ele,indx)):
-------------------------------- 
        Map is used to pass a callbackfunction to each and every elements of the array and return a value for each and Every element of the array.
eg.
let values2 = arr.map((ele,ind) => 
{
   return ele%2 == 0;
})
console.log(values2);                      //output: 

10. Filter:
---------  
        similar to map but it returns only the elements whose condition is true.
eg:
let arr=[5,4,6,3];
let x = arr.filter((e,i)=>   e>=5 );
console.log(x);                          //output: [5,6]


11. indexOf('ele'): used to return the first 
------------------
eg.
let arr= [5,4,6,3]
let arr1 = [5,4,3,4];
console.log(arr.indexOf(4));                //output: 
console.log(arr1.indexOf(4));               //output:

12. find(callbackfunction(ele,ind)):  works similarly  as filter but returns only first element that satisfies the condition   
------------------------------------  we can use alternative findIndex, findLast, findLastIndex.


eg. 
let x = arr.find((element,index) => 
{
        return ele%2==0;
})
console.log();                          //output:


 findIndex(): 
---------------

eg. 
let x = arr.findIndex((element,index) => 
{
        return ele%2==0;
})
console.log();                          //output:   



13. reverse(): used to reverse the given array.
eg. 



14. reduce(callbackfunction(prev,curr), initial_Value): It is used to find cumultive values given in array.
eg.

------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 13(DOM)
Date: 24/09/25
------------------------------------------------------------------------------------------------------------------------------------

**Object: Object is a collection of key-value pairs, where each key is indexed to specific value. Objects are represented by {}(curly braces).
---------
eg. 
        let obj =
        {
           name : "Harsh",
           age : 24
        }; 
=> To Acess:        
        console.log(obj, obj.name, obj.age);                           //using->    "." (dot method )
        console.log(obj, obj["name"], obj["age"]);                     //Using->     ["key"] (index signature)  
=> To Adding: 
        obj.email = "harsh@596@gmail.com"

=> Another way To Create Object: 
        let  x = Object.create ({name: "abc", age:21 })               //It will created but stored in prototype

=> We can Freeze(obj): 
       now we will not able to modify to obj.
       eg. 
           Object.freeze(objName)
=> We can seal the obj: 
       here we can modify(change) the obj property, but we can't add or remove any property of object.
       eg. Object.seal(objName)
            
=> Interview que:
       Difference in between Freeze() and Seal
         
-----------------------------------------------------------------------------------------------------------------------
                                                DOM
                                        (Document Object Model)
                                        ------------------------

=> Document is an object that contains complete structure of web-page in Tree like format.
   It allows to manipulate or interact with doscuments, elements, attributes or context.

Document Tree: 
-------------
                                    Windows
                                       |
                                    Document
                                       |
                                      HTML
                                       /\
                                      /  \
                                     /    \
                                    /      \  
                                Head        Body
                                  /\         /\
                                 /  \       /  \
                               meta title  h1   h2

DOM Selection: 
--------------
        We can selecte any element of document object, using following methods:
        1. getElementsByTagName('tagName'): used to selecte elements with a specific tagName and it returns an array of elements.
           eg.
              
        2. getElementsByClassName('className'): used to selecte elements with a specific className and it returns an array of elements.
           eg.

        3. getElementsByIdName('idName'): used to select single element with a specific idName and it returns particular element.
           eg.

------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 14(DOM Manipulation)
Date: 25/09/25
------------------------------------------------------------------------------------------------------------------------------------

        4. querySelector('css selector'): used to select first element that matches the specified css selector.
           Example:
                   let y = document.querySelector('.empty'); 
                   console.log(y);
                   y.textContent = "Harsh";
        5. querySelectorAll: used to select all elements that matches the specified css selector.
           Example: 
                   let z = document.querySelectorAll('.empty'); 
                   console.log(z);
                   z.textContent = "Hello Harsh";


*Events*:
---------
        Events are action that occur when a user interacts with the web-page such as: clicking a button, submitting a form, scrolling a page, hovering over an ele etc..
        Those events can be handled using event handlers such as: "onClick()".  
        Example: //Small Project 1: "Counter Project"
            
            <div class="" id="counter">
                <h1 class="" id="count">0</h1>
                <button id="btn1" onclick="incr()">Increment</button>
                <button id="btn2" onclick="decr()">Decrement</button>
                <button id="btn3" onclick="reset()">Reset</button>
            </div>

let c=0;
function incr()
{
    if(c<20)
    {
       c++;     
    }else{
        alert("Number Can't be increase after 20");
    }
    document.getElementById('count').textContent = c;
}

function decr()
{
    if(c>0)
    {
       c--;
    }else{
        alert("Number Can't be decrease after 0");
    }
    document.getElementById('count').textContent = c;
}

function reset()
{
    c=0;
    document.getElementById('count').textContent = c;
}
//----------------------------------------------------------------------------------------------


*DOM Manipulate*:
-----------------
        we can manipulate an element using below properties:
        1. textContent: used to read or change the text inside an element
        example:
               const h = document.getElementById('heading');
               function fun()
               {
                alert(h.textContent = "HII");
               }

        2. innerText: used to read or change the inside an element but its  also loads CSS so, incase of visiblity hidden in css, It can't overpass it.
        Example:
                

        3. innerHTML: used to read or change HTML inside an element.
        Example: 


        4. style: used to read or change the styles of an element.
        Example:


        5. value: used to read/select or change the value of input.
        Example:
                
------------------------------------------------------------------------------------------------------------------------------------
                                                        Day 15(DOM Manipulation...)
Date: 26/09/25
------------------------------------------------------------------------------------------------------------------------------------

        6. clasaList: used to add/remove/toggle the class names to elements.


*DOM Creation*:
---------------
        We can even create  elements using document object with methods such as:
        1. createElement('elementName'): it will create element.
        2. appendChild(childElement): it will add element to HTML.
        3. append(childElements): it will add multiple elements to HTML.
        4. 


//Task-1 Create Simple Movie Card 
